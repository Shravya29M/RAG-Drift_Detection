APScheduler>=3.10,<4
groq>=1.1
# Pinned below 1.0: the 1.x SDK removed `temperature` from the
# messages.create overloads (Anthropic dropped sampling params on current
# models), so rag/generation/llm.py fails mypy --strict against it.
# Renovate will raise the 1.x major separately — see AGENTS.md.
anthropic>=0.50,<1
faiss-cpu>=1.7
fastapi>=0.100
httpx>=0.25
numpy>=1.26
openai>=1.0
PyMuPDF>=1.23
python-dotenv>=1.0
python-multipart>=0.0.6
PyYAML>=6.0
pydantic>=2.0
scipy>=1.11
sentence-transformers>=2.2
torch>=2.0
uvicorn[standard]>=0.23
wandb>=0.16
types-PyYAML>=6.0
